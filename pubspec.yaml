name: groupscholar_verification_logbook
description: Verification logbook CLI for tracking scholar verification checks.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.10.9

# Add regular dependencies here.
dependencies:
  path: ^1.9.0
  args: ^2.7.0
  postgres: ^3.5.9
  uuid: ^4.5.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
